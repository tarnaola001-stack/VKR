meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  dir: server
  requirement:
    - package.json
  step:
    - npm install
run:
  dir: server
  command: node src/server.js